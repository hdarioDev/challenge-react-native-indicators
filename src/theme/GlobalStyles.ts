import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
    GlobalMargin: {
        marginHorizontal: 20
    },
    bgImage: {
        position: 'absolute',
        width: 300,
        height: 300,
        top: -100,
        right: -100,
        opacity: 0.2
    },
    title: {
        fontSize: 35,
        fontWeight: "bold"
    }
})